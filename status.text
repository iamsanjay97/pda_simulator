Parallel bid placing by different agent is not working as expected.

Similar to alpha-zero, all MCTS variants maintain a single Monte Carlo tree during an episode of the game (proximity 24 to 0). 
MCTS-Vanilla-V0 looks completely ready. Tested. Not planning to use V1, so didn't check.
MCTS-SPW looks completely ready. Tested.
MCTS-Cont_Regression looks completely ready and working as expected. Tested.